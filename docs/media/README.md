Pictures used by the top-level README.

- `balcony.gif` — drawing a balcony, then its sun map
- `garden.gif` — a garden with a house and a tree, shadows moving
- `land.gif` — a plot traced over a photo, including setting the scale
- `editor.png` — a still of the editor

Make the GIFs with `scripts/mov-to-gif.sh`. Keep each one under about 5 MB.
