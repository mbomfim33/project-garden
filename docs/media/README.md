Pictures used by the top-level README.

- `balcony.gif`, `garden.gif`, `land.gif` — the three at the top, 620 px wide
- `drawing.gif` — drawing a space from nothing
- `tracing.gif` — tracing a plot over a satellite image
- `sun-detail.gif` — reading one square's day
- `editor.png` — a still of the editor

Made with `scripts/mov-to-gif.sh`. The recordings themselves are gitignored.
